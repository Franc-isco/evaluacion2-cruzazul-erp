services:
  frontend:
    build: ./frontend
    container_name: cruzazul_frontend
    restart: always
    ports:
      - "80:3000"
    environment:
      DB_HOST: 10.0.2.183
      DB_USER: cruzazul_user
      DB_PASSWORD: cruzazul_pass
      DB_NAME: cruzazul_db
      DB_PORT: 5432
